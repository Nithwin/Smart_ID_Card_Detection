# Dataset configuration for Smart ID Card Detection
# Update the 'path' field to match your dataset location

path: /content/drive/MyDrive/ID_Card_Dataset  # root directory (UPDATE THIS)
train: images/train
val: images/val
test: images/test  # optional

# Class names
names:
  0: card
  1: person

# Number of classes
nc: 2
